<%- include('../partials/header') %>


<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Exercise</th>
      <th>Sets</th>
      <th>Reps</th>
      <th>Weight</th>
      <th>User</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= workout.workoutDate.toLocaleDateString() %></td>
      <td><%= workout.content %></td>
      <td><%= workout.reps %></td>
      <td><%= workout.sets %></td>
      <td><%= workout.weight %></td>
      <td class="workout-user"><img alt="avatar" src="<%= workout.userAvatar %>" referrerpolicy="no-referrer" style="border-radius: 50%; height: 60px"><%= workout.userName %></td>
      <td><a href="/workouts/<%= workout._id %>/edit">Edit my Workout</a></td>
    </tr>
  </tbody>
</table>


<h2>Comments: <% workout.comments.forEach(function(c) { %></h2>
<% if (user?._id.equals(c.user)) { %>

<h5><%= c.content %> 
    <%= c.userName %> - <img alt="avatar" src="<%= workout.userAvatar %>" referrerpolicy="no-referrer" style="border-radius: 50%; height: 60px">
</h5>
<form action="/comments/<%= c._id %>?_method=DELETE" method="POST"><input type="submit" value="Delete"></form>

<% } else { %>
  <h5>No Comments Yet</h5>
<% } %>
<% }); %>
<h2>Comment on <%= workout.userName %>'s Workout!</h2>
<form action="/workouts/<%= workout._id %>/comments" method="POST">
  <textarea name="content" id="" cols="30" rows="10"></textarea>
  <input type="submit" value="comment">
</form>
<%- include('../partials/footer') %>